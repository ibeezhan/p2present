Phase 7 done — pastebin-lite community backend. /service: Cloudflare Worker + KV (POST/GET/PUT/DELETE/report, short id + SHA-256-hashed edit token, optional expiry, public/unlisted, size cap 413, per-IP rate limit 429, /p/:id → player redirect, optional IPFS mirror behind a flag; no secrets committed). App wired: docs/src/service.js client (configurable base, default placeholder), "💾 Save & share" button → short /p/<id> link (edit token kept in-browser), ?p=<id> + /p/<id> load via the service (bundled demo names reserved). Docs: SERVICE.md (deploy guide) + HOSTING/README/DOCS updates, marked community backend. Tests: 25/25 app unit + 23/23 Worker mock-KV unit + 89/89 smoke green (390/780/1280, 0 real console errors, assets 200, static-only, light deps). Code-only — not deployed.
